<% if (site.tags.length){ %>
  <div class="widget-wrap">
    <div class="widget">
      <% function addIcon(name){ return "<i>#</i>" + name} %>
      <%- list_toppagetags({ transform: addIcon, show_count: true, minCount: theme.tag_display_min_count}) %>
    </div>
  </div>
<% } %>
